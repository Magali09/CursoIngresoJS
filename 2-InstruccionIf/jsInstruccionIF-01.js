/*Al ingresar una edad que sea igual a 15, mostrar el mensaje "niña bonita". */

function mostrar()
{
	
	//txtIdEdad

	let edad = 0;

	if (!(edad == 15)) 
	{
		console.log("niña bonita");
	}


































	/*Ejercicio 1bis: 
	/con if
	pedir a una persona que fue de vaciones , 
	la distancia que viajo y el tiempo que tardo, para calcular la velocidad
	e informar:
	60 km/hr = muy lento 
	hata 80km/h= lento 
	hasta 100= buen ritmo
	hasta 120= ahi de la ley 
	mas = eso no se hace/ */

	// let mensaje;
	// let velocidad;

	// velocidad = document.getElementById("txtIdEdad").value;
	// velocidad= parseInt(velocidad);

	// if(velocidad == 60 || velocidad == 80){
	// 	mensaje = "muy lento";
	// }else{
	// 	if (velocidad == 100) {
	// 		mensaje = "buen ritmo";
	// 	}else{
	// 		if (velocidad == 120) {
	// 			mensaje = "ahi de la ley";
	// 		}else{
	// 			if (velocidad > 120) {
	// 				mensaje = "eso no se hace";
	// 			}
	// 		}
	// 	}
	// }
	
	// 	alert(mensaje);





























	// let edad;
	// let mensaje;

	// edad = document.getElementById("txtIdEdad").value;
	// edad = parseInt(edad);
	
	// if(edad != 15){
	// 	mensaje = "ya esta grande para tener 15" ;
	// 	alert(mensaje);
	// }else{
	// 	mensaje ="niña bonita";
	// 	alert(mensaje);
	// }
	/*
	if(condicion)//verdadero o falso (0) true -> verdadero false -> falso
	{
		codigo...;
	}else
	{
		codigo...;		
	}

	operadores aritmeticos:
	+ - * / %
	operadores relacionales o condicionales
	< > <= >= == !=
	operadores logicos
    condicionUno	&&  condicionDos            ||               !
	y logica                                o logica         negacion
	*/
	
	//operadores relacionales o condicionales < (menor que), > (mayor que), <= (menor o igual que), >= (mayor o igual que), == (igual) y !=(distinto)

	/*operadores logicos && (Y) AMBAS CONDICIONES TIENE QUE SER VERDADERAS PARA USAR ESTE OPERADOR.
	|| (O letra) una de las condiciones tiene que ser verdadera
	!*/

}//FIN DE LA FUNCIÓN
//txtIdEdad

	// 	Ejercicio 1 bis bis:
	// con if
	// ingresar el nombre y los datos requeridos para calcular el IMC , 
	// e informar a la persona si es:
	// Bajo peso <18.5
	// Peso normal 18,5-24.9
	// Preobesidad 25-26.9
	// Obesidad I 27-29.9
	// Obesidad II 30-34.9
	// Obesidad III >40

	// let altura;
	// let peso;
	// let calcularIMC;
	// let mensaje;

	// peso = document.getElementById("txtIdEdad").value;
	// peso = parseInt(peso);

	// altura = prompt("Por favor, ingrese su altura");
	// altura = parseFloat(altura);

	// calcularIMC = (altura * altura)/peso